/* Reverse Array - 
Given an array, write a function that reverses its values, in-place.  
Example: reverse([3,1,6,4,2]) returns the same array, but now contains values reversed like so... [2,4,6,1,3].  
Do this without creating an empty temporary array.  (Hint: you'll need to swap values). */

const arr = [3, 1, 6, 4, 2];

reversed = (arr) => {
  return arr.reverse(); //bruuuuuhhhhh 💪
  //or use for loop with --
}

let z = reversed(arr);
console.log(z);